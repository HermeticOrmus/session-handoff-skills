# Using this repo with Cursor

The handoff discipline is tool-agnostic. The `CLAUDE.md` and `.cursor/rules/session-handoff.mdc` capture the same rules.

Copy whichever fits your tool. The HANDOFF.md template works regardless of AI coding tool — it's a discipline, not a feature.
